function setTabIndexGallery(){

  let linkNode = document.querySelectorAll("a");
  let buttonNode = document.querySelectorAll("button");
  console.log(linkNode.length+" : "+ buttonNode.length);
  console.log(linkNode)
}
/* window.addEventListener("keyup", (e)=>{
  if(e.key=="Tab")console.log(document.activeElement);
}) */